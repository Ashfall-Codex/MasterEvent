# MasterEvent Relay Server Configuration
# Écoute sur toutes les interfaces (accès via Tailscale depuis Caddy)
PORT=8765
HOST=0.0.0.0
ROOM_EXPIRY_MS=7200000
LOG_LEVEL=info
TEMPLATE_EXPIRY_MS=604800000
DATABASE_PATH=relay.db
